# Exact third-party set of the replay environment — the same pins the
# docker image installs (docker/Dockerfile), frozen from a clean install of:
#   numpy==1.26.4 tyro==0.9.17 pyyaml==6.0.3
#   cyclonedds==11.0.1 pyarrow==25.0.1 onnxruntime==1.29.0 pytest==9.1.1
#
# This is a RECORD, not the primary install path — install with
#   uv pip install -e ".[dds,parquet,encode,dev]"
#
# The VLA inference environment (torch cu128, pinned gr00t, transformers,
# flash-attn, ...) was recorded here before 2026-08-26 — recover that
# version of this file from git history together with the docker VLA
# environment.
click==8.4.2
cyclonedds==11.0.1
docstring-parser==0.18.0
flatbuffers==25.12.19
iniconfig==2.3.0
markdown-it-py==4.2.0
mdurl==0.1.2
numpy==1.26.4
onnxruntime==1.29.0
packaging==26.3
pluggy==1.6.0
protobuf==7.36.0
pyarrow==25.0.1
pygments==2.21.0
pytest==9.1.1
pyyaml==6.0.3
rich==15.0.0
rich-click==1.9.8
shtab==1.12.0
typeguard==4.6.0
typing-extensions==4.16.0
tyro==0.9.17
