"""Shared building blocks used by both the VLA runner and the session replay.

Configuration (`config`), the G1 joint tables (`g1_joints`), the ZMQ latent
protocol v4 codec (`protocol`), and the transport adapters (`io/`).
"""

__version__ = "0.1.0"
