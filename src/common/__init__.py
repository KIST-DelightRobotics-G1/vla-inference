"""Shared building blocks used by both the VLA runner and the session replay.

Configuration (`config`), the G1 joint tables (`g1_joints`), the transport
Protocols (`interfaces`), and the CycloneDDS layer (`cyclonedds/`).
"""

__version__ = "0.1.0"
